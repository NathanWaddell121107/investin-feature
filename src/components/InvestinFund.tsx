import React from "react";

interface Props {
  test?: string;
}

export const InvestinFund: React.FC<Props> = ({ test }) => {
  return (
    <div>
      <div>Investin fund container</div>
    </div>
  );
};
